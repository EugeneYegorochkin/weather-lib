export declare class Weather {
    name: string;
    country: string;
    description: string;
    icon: string;
    temp: number;
    tempMax: number;
    tempMin: number;
    pressure: number;
    humidity: number;
    constructor(name: string, country: string, description: string, icon: string, temp: number, tempMax: number, tempMin: number, pressure: number, humidity: number);
}
